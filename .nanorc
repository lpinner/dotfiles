bind ^S writeout main
bind ^Q exit main
bind ^X cut main
bind ^C copytext main
bind ^V uncut main
bind ^F whereis main
bind ^H replace main

bind ^Z undo main
bind ^Y redo main

set tabsize 4
set tabstospaces

